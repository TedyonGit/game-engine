# game-engine
A Game Engine that tries to be compatible to both platforms (Linux / Windows).


# To build the project use:
- Linux

```
./build.sh Linux
```

- Windows

```
./build.sh Windows
```

Or to clean the builds use
```
./ build.sh clean
```


Made with ❤️ by Tedy
